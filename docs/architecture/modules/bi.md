# easy-factory-bi — 看板与报表系统

## 模块定位

消费所有业务模块的数据，提供制造运营的可视化看板和报表。
BI 只读不写——不产生业务数据，只读取和聚合。

## 核心看板

### 1. 生产看板（实时）

```
┌──────────────────┬──────────────────┬──────────────────┐
│  计划完成率       │  在制品(WIP)      │  当日产出         │
│  85.3% ▲         │  12 批次         │  9,500 片        │
├──────────────────┼──────────────────┼──────────────────┤
│                                                   │
│  ▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░ 85.3%  实际/计划              │
│                                                   │
│  工序进度（甘特图）:                                  │
│  工单WO-001 ████████████░░ 80% (压片中)             │
│  工单WO-002 ██████░░░░░░░░ 40% (制粒中)             │
│  工单WO-003 ░░░░░░░░░░░░░░  0% (待开始)            │
│                                                   │
└───────────────────────────────────────────────────┘
```

### 2. 质量看板

```
┌──────────────────┬──────────────────┬──────────────────┐
│  一次合格率       │  偏差数量         │  CAPA关闭率       │
│  98.7%           │  3 OPEN          │  87.5%           │
├──────────────────┴──────────────────┴──────────────────┤
│  SPC 控制图（实时刷新）:                                 │
│  UCL ─────────────────────────────                     │
│  CL  ─────●──●───●──●──●───●─────                      │
│  LCL ─────────────────────────────                     │
│                                                        │
│  最近偏差:                                              │
│  DEV-002 压片片重偏移  MAJOR  OPEN  2026-07-12         │
│  DEV-001 水分超标      MINOR  CLOSED 2026-07-11        │
└────────────────────────────────────────────────────────┘
```

### 3. 设备 OEE 看板

```
┌──────────┬──────┬──────┬──────┬──────┬──────┬──────────┐
│ 设备      │ 可用率 │ 性能率 │ 质量率 │ OEE  │ 状态  │ 趋势     │
├──────────┼──────┼──────┼──────┼──────┼──────┼──────────┤
│ WG-001   │ 95%  │ 88%  │ 99%  │ 83%  │ RUN  │ → 稳定   │
│ MX-001   │ 92%  │ 91%  │ 98%  │ 82%  │ RUN  │ ↗ 提升   │
│ PT-001   │ 78%  │ 85%  │ 97%  │ 64%  │ IDLE │ ↘ 需关注  │
└──────────┴──────┴──────┴──────┴──────┴──────┴──────────┘
```

### 4. 仓储看板

```
┌──────────────────────────────────────────────┐
│  库存周转率:  12.5 次/月                      │
│  呆滞物料:    3 项 (> 90天未动)              │
│  待检库存:    5 批                            │
│  近效期物料:   2 项 (< 30天内过效期)           │
└──────────────────────────────────────────────┘
```

## KPI 指标定义

| KPI | 计算公式 | 数据来源 | 目标 |
|-----|---------|---------|------|
| 计划完成率 | 实际产出/计划产出 × 100% | MES | ≥ 95% |
| 一次合格率 | 一次合格数/总产出 × 100% | QMS | ≥ 98% |
| OEE | 可用率 × 性能率 × 质量率 | Equip + IoT | ≥ 85% |
| 平均维修时间(MTTR) | 总维修时间/维修次数 | EAM | < 2h |
| 平均故障间隔(MTBF) | 总运行时间/故障次数 | IoT + EAM | > 500h |
| 批次收率 | 实际产量/批量 × 100% | LIMS | ≥ 97% |
| 库存周转率 | 月出库量/平均库存 | WMS | ≥ 10 |
| 偏差关闭率 | 已关闭偏差/总偏差 × 100% | QMS | ≥ 90% |

## BI 数据层

```
                   ┌──────────────────┐
                   │   BI 看板/报表     │
                   └────────┬─────────┘
                            │
                   ┌────────▼─────────┐
                   │  数据聚合层(只读)  │
                   │  View / 物化视图  │
                   └────────┬─────────┘
                            │
          ┌─────────────────┼─────────────────┐
          │        │        │        │         │
        MES      QMS     Equip    LIMS      WMS
```

## 外部接口

```
GET    /api/bi/dashboard/production        生产看板数据
GET    /api/bi/dashboard/quality           质量看板数据
GET    /api/bi/dashboard/oee               OEE 看板数据
GET    /api/bi/dashboard/inventory         仓储看板数据
GET    /api/bi/kpi?date=2026-07            月度KPI
GET    /api/bi/reports/batch/{batchNo}    批次追溯报告
```

## 遗留问题

### 待决策
1. 前端技术栈：Vue 3 / React（与 web 模块共享还是独立部署？）
2. 实时推送：WebSocket 还是 SSE？KPI 刷新频率？
3. 报表引擎：内置 SQL 视图还是接入 BI 工具（Grafana/Superset）？
4. 移动端：是否需要移动端看板（车间主任用手机看进度）？

## AI 协作建议（2026-07-18）

BI 是数据消费层——聚合各模块数据生成 KPI 和看板。

### 推荐实施

1. **KPI 数据源映射到 core 计算器**:
   | KPI | 使用 core 工具 |
   |-----|---------------|
   | 计划完成率 | MES 工单完成数 / MPS 计划数 |
   | 批次收率 | `IBatch.getYieldPercent()` |
   | 生产提前期 | `ProductionLeadTime.calculate()` → 计划 vs 实际对比 |
   | 产能利用率 | `BottleneckDetector.detect()` + `FactoryCapacityProfile` |

2. **IReportable 集成** — core 的 `OperationResult` 已实现 `IReportable.toReport()`。BI 的报表生成可复用此接口。

3. **事件驱动更新** — KPI 不轮询计算，而是订阅领域事件增量更新：
   - `mes.batch.completed` → 更新日产量/收率
   - `qms.inspection.passed` → 更新一次合格率
   - `equip.status.changed` → 更新 OEE 可用率

